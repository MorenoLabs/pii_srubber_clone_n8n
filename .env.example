# PII Scrubber Configuration

# Server Settings
HOST=0.0.0.0
PORT=8000

# Masking Settings
MASKING_MODE=redact
MASKING_CHAR=█
MAX_TEXT_SIZE=50000

# Language Settings
SUPPORTED_LANGUAGES=en,de
DEFAULT_LANGUAGE=en
AUTO_DETECT_LANGUAGE=true

# Text Preprocessing Settings
ENABLE_PREPROCESSING=true

# Logging
ENABLE_LOGGING=false

# Basic Authentication (disabled by default)
# Set ENABLE_AUTH=true to enable authentication
# IMPORTANT: Use strong passwords (min 12 characters) in production
ENABLE_AUTH=false
API_USERNAME=admin
API_PASSWORD=change_me_to_secure_password_123

# Security Settings
MIN_PASSWORD_LENGTH=12

# Rate Limiting Settings
RATE_LIMIT_PER_MINUTE=30
RATE_LIMIT_BURST=10

# CORS Settings (restrict to specific origins in production)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CORS_ALLOW_CREDENTIALS=false

# DoS Protection Settings
MAX_PROCESSING_TIME=30
MAX_ENTITIES_PER_REQUEST=100
MAX_REQUEST_SIZE=1000000